const inputSell = document.querySelector('.sell');
const inputBuy = document.querySelector('.buy');

inputSell.addEventListener('change', updateValue); //change or keyup

function updateValue (e) {
  
}